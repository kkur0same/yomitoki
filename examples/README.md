# Examples

Rendered reading notes produced by this skill, kept as reference output.

| Example | Paper | Notes |
|---|---|---|
| [online-softmax](online-softmax/index.html) | Online normalizer calculation for softmax (Milakov & Gimelshein, 2018) | Self-contained note; uses Mermaid diagrams instead of raster figures. |

Each example is a self-contained `index.html` plus `styles.css` and `main.js`.
Open the `index.html` in a browser to view it (math renders via KaTeX and
diagrams via Mermaid, both loaded from CDN).
